[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NITRO_PRESET = "netlify"

# Nitro's Netlify preset emits the SSR handler here.
[functions]
  directory = ".netlify/functions-internal"
  node_bundler = "none"
